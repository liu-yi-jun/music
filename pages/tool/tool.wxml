<!--pages/tool/tool.wxml-->
<view catchtap="tap" class="tool">
  <navBar backgroundColorTop='rgba(0,0,0,0)' background='rgba(0,0,0,0)' iconTheme='white'></navBar>
  <view class="head">
    <view catchtap="switchBtn" data-switchBtn="musicScore" class="title {{switchBtn === 'musicScore' ? 'active' : ''}}" >
      <text class="title-top">搜索曲谱</text>
      <text class="title-bottom">Search Score</text>
    </view>
    <view catchtap="switchBtn" data-switchBtn="analysis" class="title2 {{switchBtn === 'analysis' ? 'active' : ''}}">
      <text class="title-top">调音器</text>
      <text class="title-bottom">Tuner</text>
    </view>
  </view>
  <!-- <view class="musicScoreWrap" hidden="{{switchBtn !== 'musicScore'}}"> -->
  <musicScore excludeHeight="{{excludeHeight}}" wx:if="{{switchBtn === 'musicScore'}}"></musicScore>
<!-- </view> -->
  <analysis wx:if="{{switchBtn === 'analysis' && pageShow}}"></analysis>
  <!-- <view class="wrap">
    <view class="logo-search">
      <image class="logo" src="http://use.eigene.cn/picture.png" catchtap="toAnalysis" ></image>
      <view class="searchWrap">
        <input class="search" value="{{value}}" bindconfirm="confirm">
        </input>
        <image class="searchLog" src="http://cdn.eigene.cn/search.png"></image>
      </view>
    </view>
  </view>
  <scroll-view enable-back-to-top="{{true}}" bindscrolltolower="scrolltolower" scroll-with-animation   scroll-y   style="height: calc({{excludeHeight}}px - 172rpx);">
    <view data-index="{{index}}" class="circular" wx:for="{{circulars}}" style="{{item.style}}" catchtap="goTapPractice"
      data-src="{{item.src}}">
      <view class="top top{{index + 1}}" style="{{item.TextStyle}}">
        <text class="name">{{item.tapTitle}}</text>
        <text class="author">{{item.author}}</text>
      </view>
    </view>
  </scroll-view> -->
  <showTabBar tabBarBtnShow="{{tabBarBtnShow}}">
  </showTabBar>
</view>